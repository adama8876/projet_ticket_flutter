import 'package:flutter/material.dart';

class TicketCategories extends StatelessWidget {
  const TicketCategories({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Ticket categories '),
      ),
    );
  }
}