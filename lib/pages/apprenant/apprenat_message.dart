import 'package:flutter/material.dart';

class ApprenatMessage extends StatelessWidget {
  const ApprenatMessage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Apprenat Message'),
      ),
    );
  }
}